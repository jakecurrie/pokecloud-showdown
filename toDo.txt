User auth:
- add join page to index + redesign it
- make join page redirect to /home
- add a logout button (already have user function to logout user)

General:
- redesign the pokemon card component
- add a navbar
- swap the default remix favicon.ico file for some pokemon related one (this is the icon displayed on a browser tab)

Trainers:
- come up with names, images for trainers
- seed database with trainers + their pokemon collections

Battles:
- write all server functions related to battles
- create a pre-battle page where user can first select a trainer, then select the pokemon they want to use
- create the actual battle page layout

Store:
- write server logic for generating store card bundles
- create the shop page
- make a sub component for displaying the card bundle in-store pre purchase
- make a component to display contents of a bundle post-purchase